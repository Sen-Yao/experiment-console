# Copy values to ~/.config/experiment-console/secrets.env, not into this repo.
# Keep the real file mode at 600:
#   chmod 600 ~/.config/experiment-console/secrets.env
WANDB_API_KEY=replace-with-local-secret-outside-repo
